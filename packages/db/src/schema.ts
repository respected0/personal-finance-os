/**
 * B004 intentionally defines no application tables.
 *
 * Reviewed SQL files under supabase/migrations are the only schema authority.
 */
export const schemaAuthority = "supabase-sql-migrations";
