name: Database

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      negative_migration:
        description: Add an ephemeral destructive statement and require migration policy failure
        required: false
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: database-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  migration-smoke:
    name: database / migration-smoke
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
      - name: Set up Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444
        with:
          node-version-file: .node-version
          package-manager-cache: false
      - name: Install exact pnpm
        run: npm install --global pnpm@11.18.0
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Create controlled migration failure
        if: ${{ inputs.negative_migration }}
        run: echo 'DROP SCHEMA app_identity CASCADE;' >> supabase/migrations/00000000000000_m0_foundation.sql
      - name: Run fresh migration smoke
        run: |
          set -o pipefail
          mkdir -p .tmp/ci
          pnpm db:smoke 2>&1 | tee .tmp/ci/migration-smoke.log
      - name: Verify domain coverage and float boundary
        run: |
          set -o pipefail
          pnpm test:ledger:coverage 2>&1 | tee .tmp/ci/ledger-coverage.log
      - name: Verify ledger invariants and UAT rules
        run: |
          set -o pipefail
          pnpm ledger:integration 2>&1 | tee .tmp/ci/ledger-acceptance.log
      - name: Verify P0-A1 daily core database acceptance
        run: |
          set -o pipefail
          pnpm daily:integration 2>&1 | tee .tmp/ci/daily-core-acceptance.log
      - name: Verify P0-A1 BFF acceptance
        run: |
          set -o pipefail
          pnpm daily:api:integration 2>&1 | tee .tmp/ci/daily-api-acceptance.log
      - name: Verify P0-A2 card database acceptance
        run: |
          set -o pipefail
          pnpm card:integration 2>&1 | tee .tmp/ci/card-acceptance.log
      - name: Verify P0-A2 subscription database acceptance
        run: |
          set -o pipefail
          pnpm subscription:integration 2>&1 | tee .tmp/ci/subscription-acceptance.log
      - name: Verify P0-A2 sharing and receivables database acceptance
        run: |
          set -o pipefail
          pnpm sharing:integration 2>&1 | tee .tmp/ci/sharing-acceptance.log
      - name: Verify P0-A3 reconciliation and reversal database acceptance
        run: |
          set -o pipefail
          pnpm reconciliation:integration 2>&1 | tee .tmp/ci/reconciliation-acceptance.log
      - name: Verify P0-A3 monthly report database acceptance
        run: |
          set -o pipefail
          pnpm report:integration 2>&1 | tee .tmp/ci/report-acceptance.log
      - name: Verify P0-A3 data lifecycle acceptance
        run: |
          set -o pipefail
          pnpm data-lifecycle:integration 2>&1 | tee .tmp/ci/data-lifecycle-acceptance.log
      - name: Verify P0-B1 planning database acceptance
        run: |
          set -o pipefail
          pnpm planning:integration 2>&1 | tee .tmp/ci/planning-acceptance.log
      - name: Verify P0-B2 investment database acceptance
        run: |
          set -o pipefail
          pnpm investment:integration 2>&1 | tee .tmp/ci/investment-acceptance.log
      - name: Verify P0-B3 recommendation engine database acceptance
        run: |
          set -o pipefail
          pnpm recommendation:integration 2>&1 | tee .tmp/ci/recommendation-acceptance.log
      - name: Upload migration smoke report
        if: ${{ always() }}
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f
        with:
          name: migration-smoke-report
          path: |
            .tmp/ci/migration-smoke.log
            .tmp/ci/ledger-coverage.log
            .tmp/ci/ledger-acceptance.log
            .tmp/ci/daily-core-acceptance.log
            .tmp/ci/daily-api-acceptance.log
            .tmp/ci/card-acceptance.log
            .tmp/ci/subscription-acceptance.log
            .tmp/ci/sharing-acceptance.log
            .tmp/ci/reconciliation-acceptance.log
            .tmp/ci/report-acceptance.log
            .tmp/ci/data-lifecycle-acceptance.log
            .tmp/ci/planning-acceptance.log
            .tmp/ci/investment-acceptance.log
            .tmp/ci/recommendation-acceptance.log
          if-no-files-found: error
          retention-days: 14
